node_modules
dist
dist-*
out
.DS_Store
.eslintcache
*.log*

# tsc incremental caches: both tsconfigs are composite, so a bare `tsc -p`
# (without the npm scripts' --composite false) writes one at the root
*.tsbuildinfo

# dev fixtures (generated by scripts/make-fixtures.ps1)
fixtures/

# local agent settings
.claude/
__pycache__/
