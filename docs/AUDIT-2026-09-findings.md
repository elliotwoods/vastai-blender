# Audit findings (2026-09-25)

Every finding that survived adversarial verification, by id (the `#N` in AUDIT-2026-09.md). Severity is the verifier's final call. Locations are as of `12bcfe9`, or earlier where marked.

| # | Sev | Kind | Area | Finding | Location |
|---|---|---|---|---|---|
| 0 | low | design-debt | main-core | shared/models.ts: persisted-settings fields optional only for back-compat, sentinel-typed job clips, and DB row types redeclared ad hoc in main [whole-job preview WIP, landed in 72fdf5f] | `src/shared/models.ts:419-444` |
| 1 | low | improvement | main-core | Push events are too fine-grained: one IPC message per remote log line, with an O(n) array copy per line in the renderer, and a full DB-backed snapshot on every node field update | `src/main/scheduler/scheduler.ts:368-375` |
| 2 | critical | bug | main-core | No single-instance lock: a second launch on the same profile re-provisions every node (killing in-flight renders), resets all in-flight chunks and runs a second scheduler and fleet | `src/main/index.ts:204-235` |
| 3 | critical | bug | main-core | Boot-time orphan reconcile destroys instances owned by any other profile or install on the same Vast account, including VR_MOCK / VR_USERDATA / `npm run dev` sessions | `src/main/nodes/nodeManager.ts:370-398` |
| 4 | critical | risk | main-core | Quitting the app leaves every rented instance billing with no warning; idle auto-destroy only runs in-process, and shutdown()/stop()/closeDb() are never called | `src/main/index.ts:417-426` |
| 5 | critical | bug | main-core | Spend cap is not enforced at the rent choke point: the manual 'request node' path bypasses it, the scheduler ignores the new node's price, and requestNode races past maxActiveNodes | `src/main/ipc.ts:623-625` |
| 6 | high | bug | main-core | The `alert` push channel carries every 'could not destroy instance, check the Vast.ai console!' message, and nothing in the renderer has ever subscribed to it | `src/shared/ipc.ts:117` |
| 7 | critical | bug | main-core | Path containment exists only on the media:// read side; filenames from a rented node's manifest are joined into local write paths unchecked (and can later reach shell.openPath) | `src/main/transfer/frameDownloader.ts:196-202` |
| 8 | high | bug | main-core | IPC handlers trust renderer arguments completely: chunk size 0 in the Submit dialog hangs the main process forever, and the per-keystroke Settings fields persist 'uncapped' / 1000 nodes | `src/main/ipc.ts:41-47` |
| 9 | medium | bug | main-core | Changing Project root breaks every existing preview and splits in-flight job output, because media URLs and download paths are recomputed from the current root while jobs store their own output_dir | `src/main/mediaUrl.ts:13-16` |
| 10 | high | design-debt | main-core | The headless campaign driver lives inline in index.ts, bypasses the domain API with raw SQL, and permanently rewrites the user's interactive settings; mock fixtures bloat ipc.ts | `src/main/index.ts:248-379` |
| 11 | medium | design-debt | main-core | The event bus lives in ipc.ts, so every domain module is in an import cycle and bound to Electron; the setXxxProvider injection does not achieve its stated goal and leaves the money-critical core untestable | `src/main/ipc.ts:30-39` |
| 12 | medium | design-debt | main-core | The IPC contract is not actually enforced: missing handlers are not compile errors, dead and no-op channels remain, and 17 renderer invokes are fire-and-forget with no error path | `src/shared/ipc.ts:1-6` |
| 13 | medium | risk | main-core | Secret and settings persistence fail silently: undecryptable keys still show as '(saved)', a corrupt settings.json is overwritten with defaults, and the Linux storage backend is not checked | `src/main/settings.ts:60-81` |
| 14 | medium | risk | main-core | Window hardening gaps: sandbox disabled while decoding media from untrusted machines, unfiltered openExternal, no navigation guard, no IPC sender check | `src/main/index.ts:149-152` |
| 15 | medium | risk | main-core | Boot has no failure handling and the packaged app has no log file: a DB or init error leaves an invisible process, and diagnostics go only to console.log | `src/main/index.ts:204-235` |
| 16 | high | bug | scheduler | Spend cap checks the current fleet rate, not the rate after renting; manual rents and failed-but-billing nodes bypass it entirely | `src/main/scheduler/scheduler.ts:1061-1068` |
| 17 | critical | bug | scheduler | No mid-session liveness check for nodes or agents: an unclaimed spec or a dead node leaves chunks polling forever while the node bills indefinitely | `src/main/scheduler/scheduler.ts:324` |
| 18 | critical | bug | scheduler | Idle scale-down only runs while the app is open: quitting leaves the fleet billing with no warning (Scheduler.stop and NodeManager.shutdown are never called) | `src/main/index.ts:417-426` |
| 19 | high | bug | scheduler | An abandoned ChunkRun that later throws re-requeues a chunk already re-dispatched elsewhere, causing duplicate dispatch (trigger: destroying a node mid-prep) | `src/main/scheduler/scheduler.ts:916-925` |
| 20 | medium | bug | scheduler | Chunk completion trusts the downloader's 'lost' list, so one failed final manifest read marks a chunk complete with frames never downloaded | `src/main/scheduler/scheduler.ts:418-435` |
| 21 | high | bug | scheduler | Scale policy ignores booting nodes (a cold start rents maxActiveNodes for any job), eagerFleet fights idle scale-down, and work never resets the idle timer | `src/main/scheduler/scheduler.ts:1033-1050` |
| 22 | high | risk | scheduler | One small retry budget covers every failure type, with no backoff, no node anti-affinity and no UI to retry failed chunks (job:retryMissing is a stub) | `src/main/scheduler/scheduler.ts:32-35` |
| 23 | high | bug | scheduler | Job submissions are not validated: a chunk size of 0 or less sends splitFrames into an infinite loop that crashes the main process | `src/renderer/src/screens/Jobs/SubmitJobDialog.tsx:161-166` |
| 24 | high | design-debt | scheduler | Polling over SSH per chunk is the root cause of the channel-exhaustion bugs the code keeps patching | `src/main/scheduler/scheduler.ts:31` |
| 25 | high | design-debt | scheduler | The chunk lifecycle is an implicit, unguarded state machine, and requeue changes a chunk's range under the same id that also keys its node-side files | `src/shared/models.ts:285-286` |
| 26 | high | design-debt | scheduler | scheduler.ts is a 1140-line do-everything module with circular singleton dependencies and no tests on the code paths where the bugs are | `src/main/scheduler/scheduler.ts:45-68` |
| 27 | high | bug | scheduler | Restart recovery kills and re-renders all in-flight work (the header's 're-attached' claim is false), drops node affinity, and the recovery hold misses queues with nothing in flight | `src/main/scheduler/scheduler.ts:7-9` |
| 28 | medium | risk | scheduler | The slot controller can only ramp up from its starting value, the learned starting value only ratchets upward, 'converged' is permanent per node, and backoffs lag because concurrency travels in each spec | `src/main/scheduler/slotController.ts:300-330` |
| 29 | low | bug | scheduler | Throughput learning (gpu_perf, the slot controller and the per-job cost split) counts prefetched and non-rendering runs, plus wall-clock prep time | `src/main/scheduler/scheduler.ts:126` |
| 30 | low | improvement | scheduler | Work distribution: auto chunk size ignores node packing and later fleet changes, and Blender-version affinity can starve an older job indefinitely | `src/main/scheduler/chunker.ts:13-16` |
| 31 | low | risk | nodes-vast | The extension registry trusts the manifest id (local path traversal, unescaped shell strings), leaves replaced zips behind, and its ZIP reader has no bounds checks | `src/main/addons/addons.ts:52-58` |
| 32 | critical | bug | nodes-vast | Quitting the app leaves the whole fleet billing, with no warning and no fail-safe on the node | `src/main/index.ts:417-426` |
| 33 | critical | bug | nodes-vast | Starting the app offline or during a Vast API error marks live nodes 'failed' while they keep billing, uncounted and unmetered, for the whole session | `src/main/nodes/nodeManager.ts:605-643` |
| 34 | critical | bug | nodes-vast | Orphan cleanup runs once per launch and isn't scoped to this install; destroy is a single attempt and fails on already-gone instances | `src/main/nodes/nodeManager.ts:359-361` |
| 35 | critical | bug | nodes-vast | Spend cap and max-node limits can be exceeded: the new offer's price isn't checked, manual requests skip the cap, failed-but-billing nodes aren't counted, and the check has a race | `src/main/scheduler/scheduler.ts:1031-1032` |
| 36 | high | bug | nodes-vast | Nothing detects a node dying mid-session: its chunks stay 'rendering' forever, it keeps billing, and it can swallow the queue | `src/main/nodes/nodeManager.ts:640` |
| 37 | high | risk | nodes-vast | Provisioning has no deadline, so a stalled download leaves a node 'provisioning' and billing forever | `src/main/nodes/nodeManager.ts:537-553` |
| 38 | high | design-debt | nodes-vast | The node lifecycle is an implicit state machine with many writers, and its races leak instances and bring destroyed nodes back | `src/main/nodes/nodeManager.ts:298-310` |
| 39 | high | bug | nodes-vast | Vast client: one transient 5xx or network error during boot destroys a healthy node; no timeouts, no typed errors, API key in the URL | `src/main/vast/vastClient.ts:27-70` |
| 40 | high | bug | nodes-vast | Octane: the OTOY account password is exposed in OctaneServer's command line on untrusted nodes, and the licensing/VNC flow cannot work as designed | `remote/octane/setup_octane.sh:48` |
| 41 | medium | bug | nodes-vast | The EEVEE capability probe result is recorded but never used when assigning work | `src/main/nodes/provisioner.ts:80-94` |
| 42 | high | design-debt | nodes-vast | Every app restart re-runs full base provisioning on every node, killing renders in progress | `src/main/nodes/nodeManager.ts:624-638` |
| 43 | medium | bug | nodes-vast | When scale-up fails: only one offer is tried, and each attempt adds a 'failed' row and a blacklisted machine, retried every 15s with no backoff | `src/main/nodes/nodeManager.ts:494-499` |
| 44 | medium | design-debt | nodes-vast | Offer ranking compares numbers in incompatible units, and learned throughput is keyed only by GPU name | `src/main/vast/offers.ts:128-147` |
| 45 | medium | bug | nodes-vast | Cost accrual assumes each tick is exactly one minute, so sleep and failed nodes make recorded spend wrong | `src/main/nodes/nodeManager.ts:359` |
| 46 | low | risk | nodes-vast | If the .blend header can't be read, the job silently falls back to Blender 4.5 | `src/main/blender/blendInfo.ts:49-54` |
| 47 | low | risk | ssh-transfer | Key setup shells out to ssh-keygen with no timeout or single-flight guard, and the key path has two sources of truth | `src/main/ssh/keys.ts:30` |
| 48 | low | risk | ssh-transfer | [WIP] The bundled ffmpeg-static is a 45 MB GPL-3.0 binary used only for a copy-remux, and the PATH fallback does not work in packaged macOS apps | `package.json:30` |
| 49 | critical | bug | ssh-transfer | A rented node or a .blend can write any file on the desktop: manifest filenames go into local paths unchecked (path traversal) | `src/main/transfer/manifest.ts:55` |
| 50 | high | bug | ssh-transfer | drain() treats a failed final manifest read as 'nothing left', so a chunk can be marked complete with frames never downloaded | `src/main/transfer/frameDownloader.ts:77` |
| 51 | high | bug | ssh-transfer | Local disk errors (ENOSPC, EACCES, local ENOENT) are misread as node or network problems and become paid re-renders | `src/main/transfer/frameDownloader.ts:161` |
| 52 | high | bug | ssh-transfer | An SSH connection that dies mid-session is never detected: nothing handles 'disconnected', and reconnect/recovery only run at startup | `src/main/ssh/sshConnection.ts:102` |
| 53 | high | bug | ssh-transfer | The scene is not snapshotted: every dispatch uploads the .blend as it is on disk at that moment | `src/main/jobs/jobs.ts:165` |
| 54 | high | design-debt | ssh-transfer | SSH channel and transfer limits are counted per chunk instead of per node; this structure keeps causing channel-exhaustion bugs | `src/main/transfer/frameDownloader.ts:54` |
| 55 | medium | bug | ssh-transfer | The downloader keeps the OLDEST manifest entry per filename, so files the node rewrites on a same-node retry are stale or fail verification | `src/main/transfer/frameDownloader.ts:108` |
| 56 | medium | risk | ssh-transfer | ssh2 errors are handled once on the client and not at all on SFTP; close() can leak a connection that finishes opening late | `src/main/ssh/sshConnection.ts:91` |
| 57 | medium | bug | ssh-transfer | 'Open shell' reports success without checking the terminal opened, and trusts new host keys in the user's own known_hosts | `src/main/ssh/sshTerminal.ts:49` |
| 58 | medium | risk | ssh-transfer | [WIP] Job clip rebuilds rewrite every stitched rendition in full after each quiet period (quadratic disk I/O over a job) | `src/main/transfer/jobClip.ts:33` |
| 59 | medium | bug | ssh-transfer | [WIP] Job clips permanently leave out the frames a requeued chunk had already delivered | `src/main/scheduler/scheduler.ts:1003` |
| 60 | medium | design-debt | ssh-transfer | ChunkDownloader mixes queue logic with DB, IPC and SSH singletons, has no tests, and keeps working after stop() | `src/main/transfer/frameDownloader.ts:59` |
| 61 | medium | bug | ssh-transfer | Downloads go to the CURRENT project root, ignoring the output_dir saved on the job | `src/main/transfer/frameDownloader.ts:34` |
| 62 | low | design-debt | ssh-transfer | Remote shell commands wrap values in '…' without escaping, in many places | `src/main/ssh/sftp.ts:64` |
| 63 | low | design-debt | persistence | The DB and timers are never closed on quit: closeDb(), nodeManager.shutdown() and scheduler.stop() are never called | `src/main/db/db.ts:97-100` |
| 64 | critical | bug | persistence | A 'failed' node whose destroy failed keeps billing but drops out of cost metering, the fleet rate, maxActiveNodes and the spend cap | `src/main/nodes/nodeManager.ts:694-704` |
| 65 | high | bug | persistence | The spend cap ignores the price of the node about to be rented, and manual 'add node' bypasses it entirely | `src/main/scheduler/scheduler.ts:1061-1069` |
| 66 | medium | bug | persistence | Spend is metered by counting 60 s timer ticks while the app runs, so time closed or asleep is never charged and History treats those gaps as zero spend | `src/main/nodes/nodeManager.ts:359` |
| 67 | high | risk | persistence | No single-instance lock: two processes on the same userData double-meter spend, re-dispatch each other's chunks, and each rents up to the caps | `src/main/index.ts:204-235` |
| 68 | medium | risk | persistence | Migrations can only add columns, run after schema.sql, are never tested against an old DB, and any DB open failure leaves the app running with no window | `src/main/db/db.ts:15-31` |
| 69 | medium | risk | persistence | History runs about 10 unbounded aggregate scans synchronously on the Electron main thread every 60 s, and usage data is never pruned | `src/main/history/history.ts:392-417` |
| 70 | high | design-debt | persistence | No data-access layer: raw SQL in 13 modules, a DB re-read on every node property access, and chunk/node/job state split between SQLite and in-memory maps | `src/main/nodes/nodeManager.ts:281-306` |
| 71 | low | bug | persistence | Multi-row state changes run outside transactions: cancelJob acts on a stale snapshot across awaits, and each accrual tick commits three times per node | `src/main/scheduler/scheduler.ts:1105-1133` |
| 72 | low | bug | persistence | frames.chunk_id means two different things, so a requeued chunk shows phantom progress and a wrong ETA in the Fleet node panel | `src/main/scheduler/scheduler.ts:1000-1017` |
| 73 | medium | bug | persistence | History aggregates mix windows and units: cost per frame divides windowed spend by all-time frames, average power counts ticks with no reading, and grids are grouped by raw string | `src/main/history/history.ts:275-305` |
| 74 | medium | design-debt | persistence | assets holds a never-read row per downloaded frame, is re-read whole on every frame that lands, and is scanned by chunk_id with no index (WIP adds NULL chunk_id as the job-clip marker) | `src/main/transfer/frameDownloader.ts:206-223` |
| 75 | low | design-debt | persistence | The schema enforces almost nothing, spend is stored four times, and nothing is ever deleted | `src/main/db/schema.sql:9-37` |
| 76 | critical | bug | remote-agent | Rented node can write arbitrary files on the user's desktop through a manifest `file` path containing ../ | `src/main/transfer/frameDownloader.ts:202` |
| 77 | critical | bug | remote-agent | A dispatched chunk can hold a paid node forever: dead agent means null state with no watchdog, hung Blender means a heartbeat that keeps state fresh | `src/main/scheduler/scheduler.ts:394-456` |
| 78 | high | bug | remote-agent | The end-of-render sweep manifests truncated or out-of-range files as good frames, and the app lets them overwrite verified local frames | `remote/agent/noderunner.py:781-790` |
| 79 | high | bug | remote-agent | App restart kills every in-flight render and re-dispatches full ranges; the agent re-renders already-manifested frames without re-manifesting them | `remote/provision.sh:61-73` |
| 80 | medium | risk | remote-agent | Chunk completion is never checked against the expected frame set, so 'done' plus successful downloads can still leave holes | `remote/agent/noderunner.py:692` |
| 81 | high | risk | remote-agent | Node disk is never reclaimed, and running out of space turns the agent into a relaunch loop with orphaned Blender processes | `remote/agent/noderunner.py:857-899` |
| 82 | high | risk | remote-agent | Provisioning and node prep have no timeouts, pin nothing and verify no checksums; a stalled download parks a billing node forever | `remote/provision.sh:20-31` |
| 83 | high | bug | remote-agent | GPU capability is neither verified nor enforced: Cycles silently falls back to CPU, and the EEVEE probe is display-only, 5.1-only and can destroy a good node | `remote/blender/enable_gpu.py:57-78` |
| 84 | medium | design-debt | remote-agent | The desktop-agent protocol is implicit, unversioned and duplicated by hand across Python and TypeScript, and the copies have already drifted | `remote/agent/noderunner.py:4-31` |
| 85 | high | bug | remote-agent | Octane path is broken end to end: renders silently fall back to stock Blender, license detection is inverted by its regex, re-setup spawns duplicate servers, and licenses leak | `remote/agent/noderunner.py:178-195` |
| 86 | medium | bug | remote-agent | Cancel is a `pkill -f`, not a protocol: cancelling an EEVEE chunk before its first frame lands triggers the OpenGL retry and the cancelled render continues | `src/main/scheduler/scheduler.ts:1117-1121` |
| 87 | medium | bug | remote-agent | Definitive preview clips stop at the first missing frame (frameStep > 1 gives 1-frame clips) while their metadata claims the full count; the WIP job clip trusts it | `remote/encode/encode_preview.py:35-47` |
| 88 | medium | risk | remote-agent | run_startup_scripts executes every 'startup*' text block of every .blend unconditionally, bypassing Blender's autoexec protection | `remote/blender/run_startup_scripts.py:10-13` |
| 89 | medium | bug | remote-agent | Hot loops block: FrameTracker sleeps 1s per frame and hashes inside Blender's stdout drain, and the main loop spawns nvidia-smi for every queued spec every 2s | `remote/agent/noderunner.py:144-152` |
| 90 | medium | design-debt | remote-agent | The 1054-line single-file agent has no types, no child-process cleanup and no automated tests; selfcheck.py is not run by npm test and there is no CI | `remote/agent/noderunner.py:1-1054` |
| 91 | low | bug | renderer-shell | Tooltip closes on any scroll anywhere, so auto-scrolling log consoles keep dismissing it; it is also not linked to its trigger and can't be dismissed with Escape | `src/renderer/src/components/Tooltip.tsx:98` |
| 92 | low | design-debt | renderer-shell | Custom nav store: duplicate history entries, a dead route parameter, and app-wide chrome re-mounted by every screen | `src/renderer/src/lib/nav.ts:75` |
| 93 | high | bug | renderer-shell | The 'alert' push channel is never subscribed, so billing-leak warnings ('check the Vast.ai console!') never reach the user | `src/renderer/src/lib/queries.ts:222` |
| 94 | medium | bug | renderer-shell | Double-clicking '+ request node', or clicking during a scheduler scale-up, rents past maxActiveNodes and never checks the spend cap | `src/renderer/src/screens/Fleet/FleetScreen.tsx:358` |
| 95 | medium | bug | renderer-shell | The New render dialog re-creates jobs that already succeeded after a partial multi-file failure | `src/renderer/src/screens/Jobs/SubmitJobDialog.tsx:52` |
| 96 | medium | bug | renderer-shell | Money and progress readouts go stale: the fleet rate shows $0.000/hr for up to 60 s, and Jobs list cost/progress freezes between chunk transitions | `src/renderer/src/lib/queries.ts:169` |
| 97 | medium | risk | renderer-shell | No error boundary and no query/mutation error handling: one render exception blanks the window, and production builds block reload | `src/renderer/src/main.tsx:20` |
| 98 | high | design-debt | renderer-shell | About 25 fire-and-forget ipc.invoke calls bypass the query layer: no pending state, no error reporting, no confirmation on destructive money actions | `src/renderer/src/lib/queries.ts:2` |
| 99 | medium | bug | renderer-shell | Settings fields save on every keystroke with no validation: clearing the spend cap turns it off mid-edit, idle timeout can become 0, max nodes is unclamped | `src/renderer/src/screens/Settings/SettingsScreen.tsx:135` |
| 100 | medium | bug | renderer-shell | The global Alt+Left 'back' shortcut fires inside text fields (macOS Option+Left word jump) and fights the PageBar arrow keys (PageBar part is WIP) | `src/renderer/src/App.tsx:44` |
| 101 | medium | risk | renderer-shell | Push events fan out into refetch and re-render storms: the whole-job asset index per frame, node queries per thumbnail, a store update per log line | `src/renderer/src/lib/queries.ts:279` |
| 102 | low | bug | renderer-shell | Chunk progress is computed separately on each screen and disagrees: JobDetail ignores frameStep, and live progress survives a requeue | `src/renderer/src/screens/JobDetail/JobDetailScreen.tsx:29` |
| 103 | medium | design-debt | renderer-shell | The inline-style factories in controls.ts rule out hover/active/focus states and remove the focus ring from every text input | `src/renderer/src/lib/controls.ts:168` |
| 104 | medium | bug | renderer-shell | Core navigation is mouse-only: jobs, nodes, chunks and history rows are clickable divs; the modal has no dialog semantics; the Add funds '+' sits inside a button | `src/renderer/src/screens/Jobs/JobsScreen.tsx:34` |
| 105 | critical | risk | renderer-shell | The renderer runs unsandboxed while decoding media produced by rented nodes, and window-open and navigation are unguarded | `src/main/index.ts:151` |
| 106 | medium | design-debt | renderer-shell | The event-to-cache logic, the stores and navigation have no tests; unused dependencies ship and there is no CI | `src/renderer/src/lib/queries.ts:222` |
| 107 | low | design-debt | renderer-screens | Inline-style UI with no shared component kit: disabled state set twice, no hover styles, clickable divs unreachable by keyboard, screens over 500 lines | `src/renderer/src/lib/controls.ts:47-90` |
| 108 | low | design-debt | renderer-screens | GradeLab and other dev-only code ship in the production bundle, and GradeLab changes a global flag | `src/renderer/src/App.tsx:13` |
| 109 | high | bug | renderer-screens | Chunk size 0 or negative in the submit dialog hangs, then crashes, the main process while rented nodes keep billing | `src/renderer/src/screens/Jobs/SubmitJobDialog.tsx:42` |
| 110 | critical | bug | renderer-screens | Destroying a node in the 'requested' state orphans the Vast instance that is being created, and it keeps billing unseen | `src/renderer/src/screens/Fleet/FleetScreen.tsx:270-280` |
| 111 | high | bug | renderer-screens | The renderer never subscribes to main's `alert` channel, so all 21 alerts, including 'Destroy failed — check the Vast.ai console!', are lost | `src/shared/ipc.ts:117` |
| 112 | medium | bug | renderer-screens | The Settings form writes every keystroke to disk with no draft and no validation: clearing the spend cap turns it off, and some values cannot be typed | `src/renderer/src/screens/Settings/SettingsScreen.tsx:119-125` |
| 113 | medium | risk | renderer-screens | Actions that spend or destroy money have no confirmation, no pending state and no guard: request node, destroy, cancel job, remove add-on | `src/renderer/src/screens/Fleet/FleetScreen.tsx:358-363` |
| 114 | medium | bug | renderer-screens | Multi-scene submit is not atomic, so retrying after a partial failure creates duplicate paid jobs; other input checks are also missing | `src/renderer/src/screens/Jobs/SubmitJobDialog.tsx:49-68` |
| 115 | medium | bug | renderer-screens | Gallery lazy-play set is never pruned, so the wall, including the new whole-job tile, stops autoplaying after a job switch or toggle | `src/renderer/src/screens/Gallery/GalleryScreen.tsx:25-61` |
| 116 | medium | bug | renderer-screens | JobDetail: chunk progress ignores frame step, and clicking a filmstrip frame does nothing after a requeue narrows its chunk | `src/renderer/src/screens/JobDetail/JobDetailScreen.tsx:29-33` |
| 117 | medium | bug | renderer-screens | History chart draws nothing for a single-point series and never extends a step series to 'now', which undoes main's balance anchoring | `src/renderer/src/screens/History/Chart.tsx:86-99` |
| 118 | medium | risk | renderer-screens | Telemetry causes unnecessary re-renders and refetches, and long lists are not virtualised (react-window is a dependency but unused) [react-window was removed as unused in 45b19ce; the Phase 4 virtualisation work adds it back] | `src/renderer/src/screens/JobDetail/JobDetailScreen.tsx:74-101` |
| 119 | medium | risk | renderer-screens | Settings persisted by the headless job-spec runner that affect spending are not shown in Settings: eagerFleet, cpuBound, minCpuCores, minDiskGb | `src/shared/models.ts:522` |
| 120 | medium | design-debt | renderer-screens | Business rules are computed inside components and duplicated, with three different definitions of an 'active' node | `src/renderer/src/screens/Fleet/FleetScreen.tsx:323-325` |
| 121 | medium | design-debt | renderer-screens | No handling of loading or error states: failed queries look like empty data, and most IPC calls are fire-and-forget | `src/renderer/src/main.tsx:9-17` |
| 122 | medium | improvement | renderer-screens | The UI is missing actions the docs and IPC contract describe: no VNC login for Octane, no retry for partial jobs, and several stub or unused channels | `docs/OCTANE.md:24-27` |
| 123 | low | bug | renderer-media | GradePanel's capability is guessed from `clip ? 'gl' : 'css'` instead of the grader actually in use | `src/renderer/src/media/PreviewOverlay.tsx:470-476` |
| 124 | low | design-debt | renderer-media | Dead and misleading code: unused follower sync, a broken rAF fallback, and helpers used only by tests | `src/renderer/src/media/ClipSyncController.ts:89-92` |
| 125 | medium | bug | renderer-media | HLG decode skips the HLG OOTF, so the default live preview of every EXR render is ~0.4 EV too bright at diffuse white and ~0.8 EV too bright in the midtones | `src/renderer/src/media/GradeRenderer.ts:42-48` |
| 126 | high | bug | renderer-media | Frame-exact mapping trusts clip frame counts nothing verifies; for any job with frameStep > 1 the finished preview clips hold only their first frame | `remote/agent/noderunner.py:683-684` |
| 127 | high | bug | renderer-media | Gallery wall's lazy-play slots leak on every tile unmount; after ~12 job switches or HDR/chunks toggles nothing on the wall plays | `src/renderer/src/screens/Gallery/GalleryScreen.tsx:25-61` |
| 128 | medium | bug | renderer-media | [whole-job clip] Overlay remounts mid-watch when a chunk joins the job clip, and job mode is decided from a different rendition than the one it plays | `src/renderer/src/media/PreviewOverlay.tsx:53-69` |
| 129 | high | design-debt | renderer-media | Transport position has no single owner: seeking is split across three components and depends on React effect ordering, which keeps producing bugs | `src/renderer/src/media/ClipSyncController.ts:34-59` |
| 130 | medium | bug | renderer-media | Opening the preview at a frame lands on clip index 0 when the clip wasn't loaded yet: the one-shot is spent on the placeholder controller | `src/renderer/src/media/PreviewOverlay.tsx:155-170` |
| 131 | medium | bug | renderer-media | The transport shows and accepts 0-based clip indices while every other surface uses Blender frame numbers | `src/renderer/src/media/TransportBar.tsx:34-37` |
| 132 | medium | risk | renderer-media | The whole-job asset index, including every downloaded frame row, is re-queried and re-serialised on every frame download | `src/renderer/src/lib/queries.ts:279-285` |
| 133 | medium | bug | renderer-media | The histogram reads back the full-resolution drawing buffer (up to 33 MB) every 400 ms, even while paused, and freezes during slider drags | `src/renderer/src/media/GradeRenderer.ts:359-385` |
| 134 | low | design-debt | renderer-media | The preview overlay re-renders its whole tree on every playback frame and re-subscribes listeners on every render | `src/renderer/src/media/PreviewOverlay.tsx:110-111` |
| 135 | medium | bug | renderer-media | Canvas widgets: FrameRuler redraws twice per frame and overprints its labels on long clips; CSS var() strings are silently rejected by canvas | `src/renderer/src/media/FrameRuler.tsx:29-97` |
| 136 | low | bug | renderer-media | Stepping to another chunk with [ / ] resets the HDR toggle and closes the grade drawer | `src/renderer/src/media/PreviewOverlay.tsx:64` |
| 137 | medium | design-debt | renderer-media | Grade math exists three times with no automated parity; the only shader check is manual, needs fixtures only a PowerShell script can make, and mutates a production static | `src/renderer/src/media/GradeRenderer.ts:64-176` |
| 138 | critical | bug | lens-money | Quitting the app leaves every rented instance billing until the next launch, with no warning | `src/main/index.ts:417-426` |
| 139 | critical | bug | lens-money | No liveness checks during a session: a dead agent, restarted container or dead SSH link leaves chunks polling forever and the node billing forever | `src/main/scheduler/scheduler.ts:324` |
| 140 | critical | bug | lens-money | Instances can keep billing while the app thinks they are gone or 'failed': reconciliation runs once per launch, and failed nodes are left out of every safety check | `src/main/vast/vastClient.ts:36-49` |
| 141 | high | bug | lens-money | Chunks and jobs are marked complete without checking their frames arrived; one failed manifest read at the end of a chunk loses frames for good | `src/main/transfer/frameDownloader.ts:77-95` |
| 142 | critical | bug | lens-money | A rented node can write arbitrary files on the user's machine through manifest paths | `src/main/transfer/frameDownloader.ts:197-203` |
| 143 | critical | bug | lens-money | Frames truncated by a crashed Blender are recorded as good, verified against their own hash, and never re-rendered | `remote/agent/noderunner.py:781-802` |
| 144 | high | bug | lens-money | The spend cap is not a hard limit: a single rent, a manual add or a double-click can exceed it | `src/main/scheduler/scheduler.ts:1061-1079` |
| 145 | medium | bug | lens-money | Restart recovery re-renders whole chunks, and retrying a chunk on the same node with undownloaded frames hits a stale manifest until its retries run out | `src/main/scheduler/scheduler.ts:632-646` |
| 146 | high | risk | lens-money | The startup orphan sweep destroys instances belonging to any other copy of the app on the same Vast account | `src/main/nodes/nodeManager.ts:370-398` |
| 147 | high | bug | lens-money | The startup recovery hold works only once and is easy to bypass: the next launch rents a full fleet | `src/main/scheduler/scheduler.ts:549` |
| 148 | high | bug | lens-money | Jobs render from the live .blend path, so edits after submitting produce frames from mixed scene revisions | `src/main/db/schema.sql:12` |
| 149 | high | design-debt | lens-money | Node lifecycle transitions have no rules: a restart revives nodes that were being destroyed, a reconnect skips provisioning, and there are five different destroy paths | `src/main/nodes/nodeManager.ts:308-310` |
| 150 | medium | bug | lens-money | Cost metering assumes each timer tick is exactly one minute, so it undercounts after sleep or while the app is closed and ignores failed nodes that are still billing | `src/main/nodes/nodeManager.ts:740-777` |
| 151 | medium | design-debt | lens-money | None of the money-critical paths are tested; the tests cover only the pure helpers | `src/main/nodes/nodeManager.ts` |
| 152 | low | improvement | lens-security | Secret-handling hygiene: API key also sent in the query string, Linux safeStorage fallback not detected, settings written world-readable, key generated with an ssh-keygen found on PATH | `src/main/vast/vastClient.ts:33-45` |
| 153 | low | design-debt | lens-security | Remote shell commands use three ad-hoc quoting schemes; renderer and extension-supplied identifiers reach them unvalidated | `src/main/octane/octaneLicense.ts:22-24` |
| 154 | low | risk | lens-security | Malicious extension zip or .blend: local path traversal via the manifest id, and unbounded synchronous decompression on the main thread | `src/main/addons/addons.ts:64-97` |
| 155 | critical | bug | lens-security | Node-controlled manifest filenames become local paths without a containment check: arbitrary file write, overwrite and delete on the desktop (zero-click RCE) | `src/main/transfer/manifest.ts:48-61` |
| 156 | high | bug | lens-security | OTOY account credentials are handed to the machine owner: sent in the exec command line, the environment and argv, and leaked into UI alerts on timeout | `src/main/octane/octaneLicense.ts:41-46` |
| 157 | medium | risk | lens-security | Renderer sandbox is disabled for no reason while it decodes media produced on untrusted machines, with no update channel | `src/main/index.ts:149-152` |
| 158 | high | bug | lens-security | Spend cap and max-node limits are enforced in the wrong layer: the manual rent path bypasses them, the check is racy, and the new node's price and 'failed' still-billing nodes are ignored | `src/main/nodes/nodeManager.ts:487-512` |
| 159 | medium | bug | lens-security | IPC arguments aren't validated at runtime: chunk size 0 from the Submit dialog puts the main process in an endless loop, and a free-text Blender version reaches the remote shell unquoted | `src/main/ipc.ts:45-47` |
| 160 | critical | risk | lens-security | Renderer compromise means full local code execution: unrestricted openPath/openExternal, no navigation or drop guard, no sender check, and arbitrary-path reads that upload to nodes | `src/main/ipc.ts:559-568` |
| 161 | low | risk | lens-security | Windows 'open SSH terminal' passes the host and key path through cmd.exe unescaped, and the terminal ignores the app's pinned host key | `src/main/ssh/sshTerminal.ts:22-47` |
| 162 | low | risk | lens-security | [WIP 72fdf5f] Desktop ffmpeg now parses node-supplied media with -safe 0 and a concat list that doesn't escape newlines | `src/main/transfer/jobClip.ts:155-178` |
| 163 | low | risk | lens-security | Node and build supply chain are unpinned and unchecksummed: nightly 'latest' ffmpeg, Blender tarballs, an NVIDIA .run executed as root, a mutable Docker tag, and ffmpeg-static | `remote/provision.sh:39-46` |
| 164 | low | design-debt | lens-architecture | The frame-count formula and clip-kind lists are re-derived across main, renderer and Python | `src/main/ipc.ts:145` |
| 165 | low | design-debt | lens-architecture | The composition root mixes wiring with a 160-line headless batch driver that writes raw SQL, a mutable onReady hook and stale phase comments | `src/main/index.ts:218` |
| 166 | low | bug | lens-architecture | [WIP job-clip] The preview overlay decides job mode from one rendition's segments but plays another's, and silently jumps to frame index 0 | `src/renderer/src/media/PreviewOverlay.tsx:57` |
| 167 | critical | bug | lens-architecture | No quit/teardown lifecycle and no single-instance lock: closing the app leaves the fleet billing indefinitely | `src/main/index.ts:417` |
| 168 | critical | bug | lens-architecture | Node lifecycle is an unguarded, implicit state machine; 'failed' also covers 'destroy threw, instance still billing', which slips past maxActiveNodes and the spend cap | `src/main/nodes/nodeManager.ts:308` |
| 169 | high | bug | lens-architecture | No runtime node supervision: a node that loses SSH mid-render is never detected, so its chunks hang and it bills until restart | `src/main/nodes/nodeManager.ts:640` |
| 170 | critical | bug | lens-architecture | Nothing is validated at the trust boundaries: node-supplied manifest paths become local file writes, and renderer job submissions can hang the main process | `src/main/transfer/manifest.ts:48` |
| 171 | high | bug | lens-architecture | The 'alert' push channel has no subscriber: all 25 main-process alerts, including 'check the Vast.ai console!', are silently dropped, and command errors are swallowed | `src/shared/ipc.ts:117` |
| 172 | critical | bug | lens-architecture | The spend cap and maxActiveNodes are enforced by the caller, not at the rental chokepoint: the cap ignores the new node's price, and 'add node' bypasses it entirely | `src/main/scheduler/scheduler.ts:1061` |
| 173 | medium | design-debt | lens-architecture | Domain modules import the IPC module, creating ipc↔domain cycles; module-level singletons and provider setters leave every money-critical path untestable | `src/main/nodes/nodeManager.ts:14` |
| 174 | medium | bug | lens-architecture | Side effects after each state write are hand-coded at every site and have drifted: cancelling a job never emits job:changed, and cancelled chunks are stored as 'failed' | `src/main/scheduler/scheduler.ts:1105` |
| 175 | medium | bug | lens-architecture | Three different answers to 'where does this job's output live': changing Project root splits in-flight output and breaks every existing job's media | `src/main/jobs/jobs.ts:149` |
| 176 | low | design-debt | lens-architecture | ipc.ts is three modules in one (transport, SQL read models, ~375 lines of mocks); mocking at the transport layer runs the real fleet underneath, and the contract has dead or duplicate channels | `src/main/ipc.ts:87` |
| 177 | medium | bug | lens-architecture | The app↔agent protocol is hand-mirrored in 4+ places with no version, and the duplicated slot-ceiling logic has drifted: on multi-GPU nodes the app's cap is double the agent's | `src/main/scheduler/slotController.ts:95` |
| 178 | low | improvement | lens-perf | Chunk wall mounts one <video> per chunk with no virtualisation, and tiles paused by the play cap are never resumed | `src/renderer/src/screens/Gallery/GalleryScreen.tsx:25-61` |
| 179 | low | bug | lens-perf | Log-tail channel can outlive its chunk: tailLog sets stopTail after cleanup may already have run | `src/main/scheduler/scheduler.ts:362` |
| 180 | medium | design-debt | lens-perf | [FIXED: the lane frees when Blender exits; one encode/download tail per lane] Default (exclusive) jobs leave the paid GPU idle at every chunk boundary: the next chunk waits for the encode, the full download drain and a re-prep | `src/renderer/src/screens/Jobs/SubmitJobDialog.tsx:43` |
| 181 | high | design-debt | lens-perf | Per-chunk SSH exec polling (two new channels per chunk every 5 s, prefetched runs included) uses up each node's session cap and causes the channel-contention workarounds | `src/main/scheduler/scheduler.ts:384-392` |
| 182 | medium | design-debt | lens-perf | Download throughput per node is capped by one SFTP channel's 2 MiB window, and every file is read twice and hashed on the main thread | `src/main/ssh/sshConnection.ts:244-266` |
| 183 | high | bug | lens-perf | App restart re-provisions every live node and re-renders stranded chunks from their first frame (the 'agent skips manifested frames' assumption is false) | `src/main/index.ts:218-223` |
| 184 | medium | design-debt | lens-perf | Every downloaded frame and thumbnail triggers full re-queries: assets:index grows O(n) per event (O(n^2) per job), and the assets table keeps an unused row per frame with no chunk_id index | `src/renderer/src/lib/queries.ts:266-288` |
| 185 | medium | design-debt | lens-perf | Render logs cross IPC one line per message; each line copies a 5,000-entry array and re-renders whole panels, and the buffers are never freed | `src/main/scheduler/scheduler.ts:213` |
| 186 | medium | risk | lens-perf | Agent's stdout-drain loop sleeps 1 s per pending frame and hashes it inline, capping frame recording at about 1 frame/s per slot and stalling Blender through the pipe | `remote/agent/noderunner.py:144-152` |
| 187 | medium | risk | lens-perf | [job-clip feature, new in 72fdf5f] The job clip is rebuilt by remuxing the entire job about every 20 s while chunks complete: quadratic disk I/O with a fixed 120 s ffmpeg timeout | `src/main/scheduler/scheduler.ts:959` |
| 188 | medium | design-debt | lens-perf | The .blend is re-hashed locally and with sha256sum on the node for every chunk dispatch, inside the per-node prep lock | `src/main/scheduler/scheduler.ts:222-273` |
| 189 | medium | risk | lens-perf | Main-process read paths grow with account history: N+1 job counts, an all-time cost_log SUM every minute, ten full usage_log scans per History refresh, and a sync stat on every media request | `src/main/jobs/jobs.ts:49-58` |
| 190 | medium | design-debt | lens-perf | Preview overlay re-renders its whole tree on every playback frame, and histogram sampling reads back the full-resolution buffer every 400 ms | `src/renderer/src/media/PreviewOverlay.tsx:176-179` |
| 191 | high | bug | lens-failure-ux | All 21 main-process alerts are dropped: the renderer never subscribes to the `alert` channel | `src/main/ipc.ts:50-65` |
| 192 | critical | bug | lens-failure-ux | No liveness checks mid-session: a dead instance, a dead agent or a hung Blender leaves a node 'rendering' and billing forever | `src/main/scheduler/scheduler.ts:384-392` |
| 193 | high | bug | lens-failure-ux | Provisioning failures form an invisible, unbounded rent-fail-destroy loop, and errors are misclassified | `src/main/nodes/nodeManager.ts:308-310` |
| 194 | critical | bug | lens-failure-ux | A failed destroy leaves a billing instance that is unmetered, hideable, never retried automatically, and whose only warning is dropped | `src/main/nodes/nodeManager.ts:694-704` |
| 195 | high | risk | lens-failure-ux | Costly and destructive actions have no confirmation: closing the window leaves the fleet billing, and destroy, cancel and remove-extension are one click | `src/main/index.ts:417-426` |
| 196 | high | design-debt | lens-failure-ux | No durable diagnostics: no log file, chunk failure reasons never stored, node logs lost at destroy, and the log-tail IPC is unused | `src/main/db/schema.sql:27-36` |
| 197 | high | bug | lens-failure-ux | Deterministic failures are retried blindly across the fleet, and a missing extension is skipped, so jobs can 'complete' with wrong output | `src/main/scheduler/scheduler.ts:35` |
| 198 | high | bug | lens-failure-ux | Local disk errors are misclassified: ENOSPC causes paid re-renders, and local ENOENT is reported as 'gone from the node' | `src/main/transfer/frameDownloader.ts:155-188` |
| 199 | medium | bug | lens-failure-ux | Renderer swallows failures of user actions: ~15 un-caught `void ipc.invoke`, no query error states, no ErrorBoundary, no media error handling | `src/renderer/src/screens/Fleet/FleetScreen.tsx:67` |
| 200 | high | bug | lens-failure-ux | Nothing explains why the fleet is not scaling: jobs sit 'queued' indefinitely for invisible reasons | `src/main/scheduler/scheduler.ts:1057-1079` |
| 201 | high | bug | lens-failure-ux | Recovery paths exist in main but are stubs or unwired: retry-missing, reprovision, log tail, VNC tunnel, Octane manual login | `src/main/ipc.ts:628` |
| 202 | high | bug | lens-failure-ux | Changing Project root mid-job splits output across two folders, and past jobs' previews go blank without explanation | `src/main/transfer/frameDownloader.ts:34-36` |
| 203 | low | bug | lens-failure-ux | Recovery banner flow: 'Resume' is the only exit, the hold also blocks brand-new jobs, and the banner never re-evaluates | `src/main/scheduler/scheduler.ts:654-662` |
| 204 | medium | bug | lens-failure-ux | Stale or untruthful state after crashes and requeues: zombie 'requested' node, resurrected 'destroying' node, stale progress bars, cancel shown as failure | `src/main/nodes/nodeManager.ts:349-358` |
| 205 | medium | risk | lens-failure-ux | A corrupt settings or registry file silently resets to defaults, losing the API key and disabling orphan cleanup without a message | `src/main/settings.ts:60-81` |
| 206 | low | bug | lens-tooling | Docs no longer match the code (VR_MOCK, orphan cleanup, Octane VNC fallback, heartbeat check, IPC compile-safety, licence) | `README.md:162` |
| 207 | critical | bug | lens-tooling | Rented node can write arbitrary files on the user's machine via manifest `file` paths | `src/main/transfer/manifest.ts:47-60` |
| 208 | critical | bug | lens-tooling | Quitting, closing the window, or laptop sleep leaves the whole fleet billing; nothing on either side stops it | `src/main/index.ts:417-426` |
| 209 | critical | bug | lens-tooling | Two installs, profiles or processes on one Vast account destroy each other's fleets, and max-nodes/spend caps double | `src/main/nodes/nodeManager.ts:370-398` |
| 210 | high | bug | lens-tooling | The main→renderer `alert` channel has no subscriber, so every 'check the Vast.ai console!' warning is silently dropped | `src/shared/ipc.ts:117` |
| 211 | high | design-debt | lens-tooling | Money-critical code has zero tests, and the one spending decision already overshoots the spend cap | `src/main/scheduler/scheduler.ts:1027-1099` |
| 212 | medium | risk | lens-tooling | VR_MOCK mocks only reads: the real scheduler and node manager run and mutating actions rent real machines hidden behind fake nodes | `README.md:162` |
| 213 | medium | risk | lens-tooling | No CI, and release builds depend on one machine: the move to macOS broke the Windows release path | `package.json:17-21` |
| 214 | high | bug | lens-tooling | Changing Project root in Settings splits running jobs and breaks every existing preview | `src/main/jobs/jobs.ts:149` |
| 215 | high | risk | lens-tooling | No persistent diagnostics or crash handling: silent no-window failures, swallowed cleanup errors, and a corrupt settings file wipes the API key | `src/main/index.ts:204-235` |
| 216 | high | design-debt | lens-tooling | No seams for a fake Vast API or fake node: IPC and engine modules import each other, singletons are built at import, and the base URL is hardcoded | `src/main/ipc.ts:8` |
| 217 | high | risk | lens-tooling | Electron hardening: out-of-support Electron, sandbox off, and IPC with no sender or argument checks while the renderer decodes media from untrusted nodes | `src/main/index.ts:149-152` |
| 218 | medium | design-debt | lens-tooling | Packaging, signing and dependencies: notarization silently skipped, broad entitlements, arm64-only, unused deps shipped; [WIP 72fdf5f] 45 MB ffmpeg-static | `electron-builder.yml:33-35` |
| 219 | low | improvement | lens-tooling | Type and lint strictness left on the table: noImplicitAny off (0 errors to fix), no type-aware promise rules, no remote/ linting | `tsconfig.node.json:1-8` |
| 220 | medium | design-debt | lens-tooling | Windows-first assumptions: PowerShell-only fixtures, iCloud-prone default output folder on macOS, Explorer/Ctrl wording, macOS ssh button reports success on failure | `scripts/make-fixtures.ps1:12` |
| 221 | low | bug | gap:multigpu-batch-scaling-wip | [WIP a664a18/93cbad4] Destroying a node during the new 3-minute first-connect retry marks it 'failed', blacklists the machine and can raise a false 'check the Vast.ai console' alert | `src/main/ssh/connectRetry.ts:67-69` |
| 222 | high | bug | gap:multigpu-batch-scaling-wip | [WIP 93cbad4] The lane memory guard cuts a multi-GPU node to one pinned lane within minutes and never recovers, leaving paid GPUs idle | `src/main/scheduler/gpuLanes.ts:98-106` |
| 223 | high | risk | gap:multigpu-batch-scaling-wip | [WIP batch loop, partly pre-existing] A createInstance error with an unknown outcome is treated as 'not rented': the instance can bill unseen, and the batch rents a replacement straight away | `src/main/vast/vastClient.ts:36-68` |
| 224 | medium | design-debt | gap:multigpu-batch-scaling-wip | [WIP 93cbad4] Lanes assume the queue can fill them: small jobs and the end of every job leave whole GPUs idle, where one unpinned process used to use them all | `src/main/jobs/jobs.ts:159` |
| 225 | medium | bug | gap:multigpu-batch-scaling-wip | [WIP 0089caf/93cbad4] The per-GPU throughput formula divides by every GPU even when only some were busy, so gpu_perf learns 25-75% of the real rate on multi-GPU nodes | `src/main/scheduler/scheduler.ts:505-513` |
| 226 | medium | bug | gap:multigpu-batch-scaling-wip | [WIP 93cbad4] gpu_perf and gpu_slots switched to per-GPU units with no migration; the best_slots ratchet makes old per-node values permanent, and they are now multiplied by GPU count | `src/main/db/db.ts:31` |
| 227 | medium | bug | gap:multigpu-batch-scaling-wip | [WIP 93cbad4] Batch scale-up sizes each rental with placeholder capacities, not the offers it actually rents, so it over-rents multi-GPU boxes and shared slots, and its failure cap resets every tick | `src/main/scheduler/scheduler.ts:1155-1185` |
| 228 | medium | risk | gap:multigpu-batch-scaling-wip | [WIP 93cbad4] The lane guard cannot see a per-GPU VRAM out-of-memory: it uses node-wide sampled totals, pinning balances by process count, and an OOM chunk is re-dispatched into the same lane plan | `src/main/scheduler/gpuLanes.ts:98` |
| 229 | medium | risk | gap:multigpu-batch-scaling-wip | [WIP 93cbad4] Pinning only works for Cycles: EEVEE lanes probably all share one GPU, and the agent's VRAM ceiling was raised to the sum of all cards | `remote/blender/enable_gpu.py:43` |
| 230 | medium | risk | gap:multigpu-batch-scaling-wip | [WIP 93cbad4] Lane counts match between app and agent, but GPU count and pinning are never reconciled: a failed nvidia-smi call silently turns off pinning for the agent's lifetime | `remote/agent/noderunner.py:948-972` |
| 231 | critical | risk | gap:stale-evidence-reanchor | [New angle, made worse by 93cbad4] A failed create call that Vast actually carried out is recorded as 'failed' with no instance id, so a billing GPU goes unaccounted and the batch loop rents replacements at once | `src/main/vast/vastClient.ts:37-63` |
| 232 | critical | bug | gap:stale-evidence-reanchor | [STILL OPEN @1dc753c, drain() from 994f250] A failed final manifest read counts as 'nothing lost', so the chunk and job complete with frames never downloaded | `src/main/transfer/frameDownloader.ts:96-115` |
| 233 | medium | bug | gap:stale-evidence-reanchor | [CHANGED @93cbad4, worse] A failing scale-up now makes 3 'failed' rows and blacklists 3 machines per tick with no backoff, an account-level error burns the best offers for the session, and manual and scheduler batches try to rent the same offer | `src/main/nodes/nodeManager.ts:556-590` |
| 234 | high | risk | gap:stale-evidence-reanchor | [NEW @1dc753c] The orphan-sweep fix leaves stray instances from another profile billing indefinitely, and its only warning goes to a channel nothing displays | `src/main/nodes/nodeManager.ts:397-417` |
| 235 | medium | bug | gap:stale-evidence-reanchor | [NEW @93cbad4] GPU lanes assume CUDA pinning works for every engine and read memory summed across GPUs: EEVEE lanes pile onto one card that the guards cannot see | `src/main/scheduler/scheduler.ts:228` |
| 236 | high | bug | gap:stale-evidence-reanchor | [PARTIALLY FIXED @93cbad4] A transient Vast 5xx, a network error, or a 5th 429 during the boot poll still destroys a healthy node, and the 8-per-tick ramp raises the 429 load | `src/main/nodes/nodeManager.ts:641-654` |
| 237 | low | bug | gap:stale-evidence-reanchor | [NEW @93cbad4] Scale-up assumes new nodes have minNumGpus GPUs, but rents whatever ranks best and counts nodes rather than lanes | `src/main/scheduler/scheduler.ts:1164-1194` |
| 238 | medium | bug | gap:stale-evidence-reanchor | [NEW @0089caf/93cbad4] gpu_perf and gpu_slots switched to per-GPU units with no migration: old per-node rows are multiplied by GPU count, and gpu_slots only ever increases, so the over-seed never corrects | `src/main/vast/offers.ts:101-118` |
| 239 | medium | bug | gap:stale-evidence-reanchor | [NEW @93cbad4] Destroying a node during its 3-minute SSH retry is recorded as a node failure, and an attempt that succeeds late can bring the node back as 'provisioning' | `src/main/ssh/connectRetry.ts:66-69` |
| 240 | low | risk | gap:transfer-rewrite-994f250 | Resume treats any shorter .part as a prefix of the current file, and a deterministic EOF is retried as transient: stale or holey partials waste attempts, and .part files are never cleaned up | `src/main/ssh/sftp.ts:150-161` |
| 241 | low | design-debt | gap:transfer-rewrite-994f250 | The reset, resume and drain-budget behaviour added in 994f250 has no tests, and the reader test's fake SFTP cannot model the channel-reset semantics behind the failures above | `src/main/ssh/pipelinedGet.test.ts:8-46` |
| 242 | high | bug | gap:transfer-rewrite-994f250 | The 10-minute drain budget counts wall-clock time, not progress: slow but healthy final downloads are cut off, and their frames are re-rendered (and paid for) until MAX_RETRIES fails the chunk | `src/main/transfer/frameDownloader.ts:37` |
| 243 | medium | bug | gap:transfer-rewrite-994f250 | A drain timeout abandons live transfers that keep writing: a late finish marks a requeued frame 'downloaded', and the re-render's download shares the same .part, so each side's rm or rename deletes or corrupts the other's file | `src/main/transfer/frameDownloader.ts:78-81` |
| 244 | high | risk | gap:transfer-rewrite-994f250 | A reset can leave a scene or add-on upload waiting forever: uploadFileVerified keeps the SFTP wrapper across two exec awaits, and ssh2 silently drops requests on a closed channel, so the node's prep lock is never released | `src/main/ssh/sftp.ts:107-116` |
| 245 | medium | risk | gap:transfer-rewrite-994f250 | resetSftp() ends whichever channel is cached, not the one that stalled: simultaneous stalls cascade into the replacement channel, unrelated downloads, uploads and spec writes use up MAX_ATTEMPTS and MAX_RETRIES, and the open-timeout reset does nothing | `src/main/ssh/sshConnection.ts:249-266` |
| 246 | high | bug | gap:scene-dependency-preflight | No scene-dependency preflight: unpacked textures, linked libraries, caches and fonts render wrong frames that complete and are billed across the whole fleet | `src/main/scheduler/scheduler.ts:236` |
| 247 | high | bug | gap:scene-dependency-preflight | Chunked rendering silently breaks unbaked simulations, and disk caches can never be found because the .blend is renamed | `src/main/jobs/jobs.ts:159-160` |
| 248 | medium | bug | gap:scene-dependency-preflight | The Engine picker is only a label: the scene's saved engine renders, while the label alone decides the EEVEE OpenGL retry | `src/renderer/src/screens/Jobs/SubmitJobDialog.tsx:38` |
| 249 | medium | bug | gap:scene-dependency-preflight | A movie output format or 'File Extensions' off in the scene turns every chunk into a 5-attempt paid retry loop | `remote/agent/noderunner.py:74` |
| 250 | low | design-debt | gap:scene-dependency-preflight | The documented remedy (pack resources) cannot cover libraries, sequences, movie clips or caches, and the remote layout rules out shipping dependencies | `README.md:101-102` |
| 251 | medium | improvement | gap:scene-dependency-preflight | Nothing inspects the scene at submit: the dialog asks for engine, range and output blind and cannot warn about dependencies | `src/main/blender/blendInfo.ts:57-80` |
| 252 | medium | improvement | gap:scene-dependency-preflight | Blender's load-time warnings (missing library IDs, fonts, OCIO view/look fallbacks) reach the agent and are discarded | `remote/agent/noderunner.py:753-762` |
| 253 | medium | risk | gap:unmetered-vast-charges | Offer selection is blind to bandwidth price, and its candidate pool and uplink bonus both favour hosts that could charge for it | `src/main/vast/types.ts:1` |
| 254 | medium | risk | gap:unmetered-vast-charges | Transfer bytes are known exactly but never metered: the spend cap, History, jobs.cost_so_far and costPerFrame all exclude traffic | `src/main/ssh/sftp.ts:131-192` |
| 255 | low | risk | gap:unmetered-vast-charges | Every money control uses the search-time quote: it is priced for no stated disk size and is never corrected from the rented instance's own dph_total | `src/main/vast/offers.ts:29-52` |
| 256 | medium | improvement | gap:unmetered-vast-charges | The real Vast balance is recorded but never compared with metered spend, and the only 'actual $/hr' readout is circular, with a tooltip that explains the gap away | `src/main/nodes/nodeManager.ts:225-231` |
| 257 | low | bug | gap:unmetered-vast-charges | Live preview re-sends the whole clip so far on every update: billed traffic grows with the square of chunk length while a chunk is watched | `remote/agent/noderunner.py:527-536` |
| 258 | medium | improvement | gap:unmetered-vast-charges | No credit-runway guard: the scheduler keeps renting at full fleet size whatever credit is left, and the only low-balance signal is a fixed $5 colour | `src/main/scheduler/scaling.ts:30-63` |
